/* 
 * method11- public int lastIndexOf(int ch, int fromIndex); 
 *
 * description- 
 * -finds the last instance of the character in the given String 
 *
 * parameters: character (ch to find), Integer (index to start the search). 
 * return Type: Integer
 */
